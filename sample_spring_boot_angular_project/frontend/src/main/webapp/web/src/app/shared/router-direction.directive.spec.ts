import { RouterDirectionDirective } from './router-direction.directive';

describe('RouterDirectionDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterDirectionDirective();
    expect(directive).toBeTruthy();
  });
});
